specVersion: "v1"
serviceId: "gen-lang-client-0393063451-service"
location: "europe-west1"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "gen-lang-client-0393063451-2-database"
        cloudSql:
          instanceId: "gen-lang-client-0393063451-2-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./example"]
